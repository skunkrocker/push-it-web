-- 1° create app table
CREATE TABLE APPLICATION
(
    APP_NAME    VARCHAR(500),
    CERT_PASS   VARCHAR(500),
    CERTIFICATE VARCHAR(2147483647),
    ID          BIGINT GENERATED BY DEFAULT AS IDENTITY,
    PRIMARY KEY (ID)
);
